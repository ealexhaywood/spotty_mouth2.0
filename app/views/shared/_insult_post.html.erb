<%= form_for [@user, @insult] do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :content, :class => 'objectPoster' %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>